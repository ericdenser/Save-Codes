spring:
  application:
    name: bff-project
 
  data:
    redis:
      host: localhost
      port: 6379
 
  security:
    oauth2:
      client:
        registration:
          keycloak:
            client-id: spring-bff
            client-secret: to1pYVUBQAJiYqzVul4GhaM1t7y2VbRa
            scope: openid, profile, email
            authorization-grant-type: authorization_code
            redirect-uri: "{baseUrl}/login/oauth2/code/{registrationId}"
        provider:
          keycloak:
            issuer-uri: http://localhost:8080/realms/bff
  session:
    store_type: redis
 
server:
  port: 8081
 
logging:
  level:
    org:
      springframework:
        security: DEBUG
        oauth2: DEBUG
        web:
          client:
            RestTemplate: DEBUG
